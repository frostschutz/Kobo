Debug tool for developers. Not recommended for users.

When USB is connected,

display a happy face if /mnt/onboard and /mnt/sd were umounted properly.
display a sad face if still mounted for some reason.

An indicator for the reason can be found in /tmp/UsbDebug.log
